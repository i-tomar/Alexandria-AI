providers = ["python"]

[phases.install]
cmds = ["python -m venv /opt/venv", "/opt/venv/bin/pip install -r backend/requirements.txt"]

[start]
cmd = ". /opt/venv/bin/activate && uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
